@model Pana.Api.Web.Components.EmptyStateProps

<div class="text-center py-16">
    <div class="mx-auto w-20 h-20 bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl flex items-center justify-center mb-5">
        <svg class="w-10 h-10 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                  d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
        </svg>
    </div>
    <h3 class="text-lg font-semibold text-gray-700 mb-1">@Model.Title</h3>
    <p class="text-sm text-gray-400 mb-6 max-w-xs mx-auto">@Model.Description</p>
    @if (Model.ActionLabel != null && Model.ActionHxGet != null)
    {
        @await Component.InvokeAsync("Button", new {
            text = Model.ActionLabel,
            variant = "primary",
            hxGet = Model.ActionHxGet,
            hxTarget = Model.ActionHxTarget
        })
    }
</div>
